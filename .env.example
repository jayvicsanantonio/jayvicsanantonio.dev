# Copy to `.env.local` to configure local development toggles.

# Partial Pre-rendering (PPR) is enabled by default when cache components are on.
# Set to "false" to disable per-route `experimental_ppr` flags during debugging.
# PPR_ENABLED=false
